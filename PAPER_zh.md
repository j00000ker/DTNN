# DTNN 论文总结

[English](PAPER.md) | **中文**

> Lai, S., Wang, M., Zhao, S., & Arce, G. R. (2023). Predicting High-Frequency Stock Movement with Differential Transformer Neural Network. *Electronics, 12*(13), 2943. https://doi.org/10.3390/electronics12132943

## 问题背景

从限价订单簿（LOB）数据中预测高频股票走势。LOB 数据噪声极大、信噪比极低——相邻时间片之间的细微差异很容易被状态向量本身的数量级淹没。现有方法大多让模型直接从原始（或归一化后）的数据中学习模式，但由于金融数据的非平稳性和低信噪比，这很困难。

## 模型架构

DTNN 由三个模块组成：

### 1. 特征提取器

两条并行去噪分支 + 恒等残差：

- **TABL**（时序注意力增强双线性层）：`X̃ = β(X ⊙ A) + (1−β)X`，`Y = ReLU(X̃ W₂ + B)`。通过二维注意力在时间和特征轴上学习双线性投影，捕捉全局序列模式。
- **TCN**（时序卷积网络）：因果膨胀卷积（dilation = 2ⁱ），提取局部特征并抑制噪声。核大小 k=2，三层 channel=[120, 120, 120]（当 dim=60 时）。
- **恒等直通**：保留原始信号。

三路输出拼接后通过线性层投影到统一维度（权重用 `eye_` 初始化，使训练初期原始信号直接通过）。

### 2. 差分层（核心贡献）

将特征序列从绝对状态 `(s₁, s₂, ..., sₙ)` 转换为差分形式 `(s₁, Δ₂, Δ₃, ..., Δₙ)`，其中 `Δᵢ = sᵢ − sᵢ₋₁`。

**数学原理：** 在高频 LOB 数据中，相邻状态向量的差异 `Δᵢ` 远小于 `sᵢ`。标准 Transformer 中注意力系数 `c(sᵢ + pᵢ)` 被位置嵌入主导，模型主要学习位置模式而非状态变化。差分后注意力变为 `c(Δᵢ + pᵢ − pᵢ₋₁)`，`Δᵢ` 不再可忽略，迫使模型捕捉真实的状态转换。

**单位根检验：** 原始数据仅 6% 通过平稳性检验 → 特征提取后 82.2% → 差分层后 **98.3%**。

### 3. 预测 Transformer 模块

可学习的 CLS token 和位置嵌入 → 多头自注意力（depth=3, heads=32, dim_head=64）→ 仅提取 CLS 输出 → MLP（LayerNorm → Linear → 激活）→ 3 类分类。

## 实验设置

### 数据集

**FI-2010**：公开 LOB 基准，5 只纳斯达克北欧股票，10 天，约 400 万事件。每时间片 40 维特征（10 档 × {卖价, 卖量, 买价, 买量}）。T=100 片，预测周期 k=10, 20, 50, 100。前 7 天训练/验证，后 3 天测试。

**中国真实股票数据**：两组实验——组 1 用 10 只股票 7 天训练/3 天测试；组 2 用 100 只股票 10 天训练，另 5 只股票 5 天测试。

### 标签定义

`pₜ = (pₐ₍₁₎(t) + p_b(1)(t)) / 2`（中间价），`m₊(t) = (1/k) Σ_{i=1}^{k} p_{t+i}`（未来 k 步均价），`lₜ = (m₊(t) − pₜ) / pₜ`（平均收益率）。设阈值 α：`lₜ > α` → 上涨；`−α < lₜ < α` → 不变；`lₜ < −α` → 下跌。

### 基线模型

SVM、MLP、CNN、LSTM、CNN-LSTM、TABL、DeepLOB、DeepLOB-Attention。

## 实验结果

### FI-2010（F1 分数，%）

| 模型 | k=10 | k=20 | k=50 | k=100 |
|---|---|---|---|---|
| SVM | 59.07 | 49.57 | 47.79 | 38.05 |
| MLP | 66.26 | 57.23 | 51.74 | 47.03 |
| CNN | 73.84 | 64.97 | 66.65 | 65.22 |
| LSTM | 67.90 | 57.68 | 57.25 | 55.64 |
| CNN-LSTM | 74.50 | 64.78 | 66.45 | 66.41 |
| TABL | 77.63 | 66.93 | 78.44 | — |
| DeepLOB | 83.40 | 72.82 | 80.35 | 76.76 |
| DeepLOB-Attention | 82.37 | 73.73 | 79.38 | 81.49 |
| **DTNN** | **86.92** | **77.14** | **87.94** | **92.53** |

DTNN 在所有周期上均优于其他方法。k=100 时 F1 比 DeepLOB-Attention 高 11.04%。

### 消融实验（F1，%）

移除差分层后 F1 大幅下降，周期越长影响越大：

| 周期 | 完整 DTNN | 无差分层 | 降幅 |
|---|---|---|---|
| k=10 | 86.92 | 78.88 | −8.04 |
| k=20 | 77.14 | 67.00 | −10.41 |
| k=50 | 87.94 | 71.05 | −16.89 |
| k=100 | 92.53 | 70.39 | −22.14 |

移除特征提取器或 Transformer 模块同样导致显著下降。

### 真实股票数据

组 1（同分布）：F1 85.79% / 77.62% / 66.09% / 62.08%（k=10/20/50/100）。
组 2（跨股票泛化）：F1 82.53% / 73.98% / 66.60% / 59.10%。

## 实现细节

训练使用 Adam 优化器，学习率 1e-4，CrossEntropyLoss，batch_size=64，150 epochs。模型返回原始 logits。

代码中 `prepare_x` 额外生成了 20 个两两交互特征（40→60 维），这是论文中未描述的增强，可能带来小幅性能提升。
